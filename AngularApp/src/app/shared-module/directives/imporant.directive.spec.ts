import { ImporantDirective } from './imporant.directive';

describe('ImporantDirective', () => {
  it('should create an instance', () => {
    const directive = new ImporantDirective();
    expect(directive).toBeTruthy();
  });
});
